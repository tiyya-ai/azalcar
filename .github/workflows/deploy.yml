name: Deploy to Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # مهم جداً لمعرفة الملفات التي تغيرت

      - name: 🚀 Sync Changed Files to FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.5
        with:
          server: 193.203.162.199
          username: admin_azal
          password: ${{ secrets.FTP_PASSWORD }}
          # هذه الأداة سترفع فقط الملفات التي تغيرت منذ آخر commit ناجح
          local-dir: ./
          server-dir: /laravel_app/
          exclude: |
            .git*
            .github*
            node_modules/
            vendor/
            tests/
            storage/logs/
            *.sqlite
